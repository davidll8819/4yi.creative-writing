# 焕新 Beauty AI：单机版第一版

当前已完成手机版交互视觉切片，入口为 `index.html`。

目标功能：老板密码、API 设置、本地 TXT/Word/PDF 知识库、员工问答、销售话术、面试评分、数据备份恢复。

当前环境检测到 Node.js，但未检测到 Java、Gradle、Android SDK 或 adb，因此暂时不能在本机直接生成 APK。下一步需要补齐 Android 打包工具链，再把这个本地页面封装为 APK。

设计原则：数据保存在公用手机本地；API 只在老板界面配置；员工不能查看配置；所有数据提供导出备份和导入恢复。
